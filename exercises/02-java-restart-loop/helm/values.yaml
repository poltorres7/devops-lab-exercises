image:
  repository: registry.digitalocean.com/devopslabregistry/java-restart-loop
  tag: latest
  pullPolicy: IfNotPresent

nodeSelector:
  workload: lab

tolerations:
  - key: dedicated
    operator: Equal
    value: lab
    effect: NoSchedule
